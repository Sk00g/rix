const PanelColor = Object.freeze({
    BLUE: "blue",
    ORANGE: "orange",
});

const PanelSize = Object.freeze({
    LARGE: "large",
    SMALL: "small",
});

export default {
    PanelColor: PanelColor,
    PanelSize: PanelSize,
};
