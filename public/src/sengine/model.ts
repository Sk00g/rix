export enum Direction {
    Left = "Left",
    Right = "Right",
    Up = "Up",
    Down = "Down",
}

export enum Animation {
    Walk = "Walk",
    Stand = "Stand",
    Attack = "Attack",
}

export type Point = [number, number];
